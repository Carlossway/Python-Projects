# Instructions for Running the Notebooks

## Prerequisites
Ensure you have the following installed on your system:
- Python 3.8 or above
- pip (Python package installer)

## Steps

### 1. Set Up the Virtual Environment
1. Navigate to the directory where this project is stored.
2. Create a virtual environment by running:
   ```bash
   python -m venv myenv
   ```
3. Activate the virtual environment:
   - On Windows:
     ```bash
     .\myenv\Scripts\activate
     ```
   - On macOS/Linux:
     ```bash
     source myenv/bin/activate
     ```

### 2. Install Required Libraries
Run the following command to install necessary libraries:
```bash
pip install pandas openpyxl ipykernel seaborn matplotlib
```

### 3. Open and Run the Notebooks

1. Open the following notebook in your browser:
   - `Matplotlib_Real_World_Scenarios_Activity.ipynb`
2. Follow the instructions to complete the activities.

### 4. Deactivate the Virtual Environment
Once done, deactivate the virtual environment by running:
```bash
deactivate
```

## Additional Notes
- Refer to the README.md for an overview of the project and learning objectives.

